<%= form_with(model: original_post) do |form| %>
  <% if original_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(original_post.errors.count, "error") %> prohibited this original_post from being saved:</h2>

      <ul>
        <% original_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
